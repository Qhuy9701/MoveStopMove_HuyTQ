using UnityEngine;

public class LevelManager : Singleton<LevelManager>
{
    [SerializeField] private int currentLevel;

    // Goto the specified level by spawning bots
    public void GoToLevel(int level)
    {
        
    }
}
